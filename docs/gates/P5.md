# P5 — Integrációk szimulációval (e-mail, Google-értékelések) — bizonyítékok

Ág: `feat/p5-integraciok` (worktree: `gyurusi-menes-wt-p5`, alap: 67aaf1f). Port: 3041. Dátum: 2026-09-14.
Minden futás a worktree-ben, friss buildből; a helyi DB előtte és utána `npm run db:reset`.

## Ennek a futásnak a helyzete (újrafuttatás)
- A P5 munkája egyszer már elkészült (471c622), és a main-be is bekerült (a1c552a). Ez a futás a worktree-t a kért alapról (67aaf1f) hozta létre, az ágat **fast-forwarddal a 471c622-re** léptette — új, párhuzamos változat szándékosan nem készült, mert az ütközne a már egyesített kóddal —, majd minden kaput és ellenőrzést **újra lefuttatott** ezen a forráson.
- Egy javítás került az ágra: `scripts/checks/_p5-harness.mjs` `cleanEnv()`. A Next a `.env.local`-ból azt a kulcsot tölti be, amelyik a folyamatban nem létezik — a változók törlése ezért nem elég, a fő repó `.env.local`-jának `CONTACT_TO`/`CONTACT_FROM` értéke beszivárog a próbába. Most az integrációs változók üres értéket kapnak (a kód mindenhol `?.trim() ||`/`!key`-t használ, az üres = beállítatlan). Ugyanez a sor az egyesítéskor már a main-re is felkerült, a két változat bájtra azonos.
  - Mutációs kontroll (ebben a futásban): a worktree-ben ideiglenes `.env.local` (`CONTACT_TO=p5-szivargas@example.com`), a régi harness-szel → `FAIL: a címzett nem az info@gyurusimenes.hu (CONTACT_TO nélkül): ["p5-szivargas@example.com"]`; a javított harness-szel ugyanezzel a `.env.local`-lal → `PASS: p5-mail` (napló sha256 47aff7e67971a79b…). A fájl utána törölve.

## Kapuk

| Kapu | Parancs | Eredmény |
|---|---|---|
| G21 | `node scripts/checks/p5-mail.mjs` | exit 0, `PASS: p5-mail` (napló sha256 1321ad89bfcda4f7…) |
| G22 | `node scripts/checks/p5-reviews.mjs` | exit 0, `PASS: p5-reviews` (napló sha256 58fbd78dded1c0f7…) |

### G21 — mért lépések (ez a futás, `.env.local` nélkül)
- Resend-mock (200) + `next start` a 3041-en: `RESEND_API_KEY=teszt`, `RESEND_API_BASE=<mock>`, `CONTACT_TO`/`CONTACT_FROM` nélkül.
- Kapcsolati üzenet → 1 levél, `to: ["info@gyurusimenes.hu"]`, `reply_to: p5-kuldo@example.com`, tárgy: „Új üzenet a weboldalról (Túrák) – P5 Küldő”, feladó „Gyűrűsi Ménes <weboldal@gyurusimenes.hu>”, `Authorization: Bearer teszt`.
- Jelentkezés e-maillel hu/en/de → nyelvenként 2 levél: magyar értesítő az info@-ra (név, létszám, telefon, válaszcím a jelentkező), visszaigazolás csak a jelentkezőnek:
  hu „Megkaptuk a jelentkezésed – P5 próba-esemény”, en „We received your registration – P5 test event”, de „Wir haben Ihre Anmeldung erhalten – P5 Testveranstaltung”; a törzs nyelve ellenőrizve, a másik két nyelv jelölői nincsenek benne; a visszaigazolás válaszcíme a ménes.
- Mock 500 → a kapcsolat és a (német) jelentkezés 200/ok, a mock 3 elbukott küldést látott, az adat a `data/site.json`-ban (a keresés a sosem küldött jelölőt nem találja — kontroll).
- A mock leállítva (hálózati hiba) → siker + tárolás.

### G22 — mért lépések (ez a futás)
- A) build kulccsal, fájl-driver: a `/`, `/en`, `/de` HTML-jében `data-reviews="idle"` váz, Google-adat nincs, JSON-LD van és nincs benne aggregateRating/Review. Betöltéskor 0 hívás; 899,7 px-re a blokktól 0; 600 px-en belül pontosan 1 (`details`, `hu`, a szerver kulcsával, FieldMask `rating,userRatingCount,googleMapsUri,reviews`).
- DOM: „Google Maps” 14 px, 400, normál, `rgb(94, 94, 94)`, `nowrap`, `translate="no"`; 5 avatar `loading="lazy"`, `referrerpolicy="no-referrer"`, ténylegesen betöltve; „Próba Szerző 1” a profil-linkkel; relatív idő; link a googleMapsUri-ra.
- Mobil 390 px, angolul: `scroll-snap-type: x mandatory`, a lista 1553/350 px, oldalra lapozható, a blokk nem csordul túl; „Translated by Google” + az eredeti szöveg gombra.
- `/api/reviews`: `Cache-Control: private, no-store, max-age=0`; a Google 500-ára `/api/reviews` 502 és a blokk eltűnik.
- Tár: `data/google/` csak `daily-calls.json` (2026-09-14: 4 = a hívások száma); véleményszöveg, darabszám, szerzőnév sehol a `data/`, `.next/server`, `.next/cache` alatt. Pozitív kontroll: ugyanez a keresés a mock valódi HTTP-válaszát tartalmazó fájlban mindhárom kifejezést megtalálja (3 találat).
- B) Netlify Blobs-szimulátor (`@netlify/blobs/server`), place ID nélkül, `GOOGLE_REVIEWS_DAILY_CAP=1`: első betöltés `search,details`, a place ID a Blobs „google” tárában (`entries/p5-site/site:google/place-id`), `data/google` nem jött létre; második betöltés 429, a blokk eltűnt, új hívás nincs; újraindítás 5-ös kerettel: csak `details` (a tárolt place ID-vel); a Blobs „google” tárában 4 fájl, a számláló 2 (`entries/p5-site/site:google/daily-calls`); vélemény és darabszám sehol a Blobs-tárban és a `data/`-ban.
- C) build kulcs nélkül: nincs váz a HTML-ben, végiggörgetve 0 hívás, `/api/reviews` 404 hívás nélkül, nincs aggregateRating.

### A próbák el tudnak bukni (mutációs kontroll ebben a futásban; utána `git checkout` a mutált fájlra, `git diff --quiet` → bájtra visszaállítva, a `data/reviews-cache.json` törölve)
- a) a 67aaf1f-es `mail.ts` (CONTACT_TO nélkül nem küld) → exit 1, `FAIL: a kapcsolati üzenetre nem pontosan egy levél ment (0)`
- b) a `loadGoogleReviews` a választ a `data/reviews-cache.json`-ba is kiírja → exit 1, `FAIL: Google-tartalom a tárban: data/reviews-cache.json` (P5MOCK, a darabszám önálló számként, „Próba Szerző”)
- c) azonnali betöltés (`NEAR_PX = 100000`) → exit 1, `FAIL: betöltéskor hívás történt (mock: 1, /api/reviews: 1)`
- d) szivárgó `.env.local` a régi harness-szel → `FAIL` (lásd fent, „Ennek a futásnak a helyzete”)

A mutációk után friss `npm run build` (exit 0, `ƒ /api/reviews`) és `npm run db:reset`.

## Egyéb futtatott ellenőrzések (ez a futás)

| Parancs | Eredmény |
|---|---|
| `npx tsc --noEmit` | TSC_OK |
| `npx eslint` (= `npm run lint`) | exit 0, üres kimenet — a harness-javítás előtt és után is |
| `npm run build` | exit 0, „Compiled successfully”; útvonal: `ƒ /api/reviews` |
| `node scripts/verify.mjs css-motion` | PASS: css-motion |
| `node scripts/verify.mjs i18n` | PASS: i18n |
| `node scripts/verify.mjs content-no-fabrication` | PASS: content-no-fabrication |
| `PORT=3041 node scripts/with-server.mjs node scripts/verify.mjs http` | PASS: http |
| `PORT=3041 node scripts/with-server.mjs node scripts/admin-flow.mjs` | ADMIN_FLOW_OK |
| `PORT=3041 node scripts/with-server.mjs node scripts/qa-flow.mjs` | QA_FLOW_OK |

A http/admin-flow/qa-flow a harness-javítás előtt futott; a javítás csak a P5-próbák segédfájlját érinti, azokat nem használják.

## Megjegyzések
- Mobilon a főoldal egésze 447/390 px széles — ezt a térkép-blokk (`div.map`) okozza, az átnézés B14-es lelete, a P4 (G25) javítja. A G22 ezért az értékelés-blokk saját túlcsordulását méri (0 px), a lap egészét csak naplózza.
- A tár-keresés első változata hamis találatot adott: a darabszám (48213) egy Turbopack-gyorsítótárbeli hex-hash részleteként szerepelt. A darabszámot azóta önálló számként keressük (hex-karakter nem állhat mellette); a kontroll továbbra is megtalálja.
- A `GOOGLE_PLACES_KEY` a build idején is kell: a váz a lap renderelésekor dől el (a próba ezért kulccsal és nélküle is buildel).
- A main-en (c79f1ad) a `src/lib/mail.ts` első két sora az egyesítéskor megduplázódott (`/**` + „E-mail küldés…” kétszer). Lefordul, mert a belső `/**` a megjegyzés része, de az egyesítő ágensnek érdemes kijavítania; ezen az ágon a fájl hibátlan.
